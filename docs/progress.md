# NIVAAN — Execution Progress Log

## Current Status
- Current phase: **Phase 6 chain ADAPTERS now FULLY WIRED (session 5): the sign + broadcast + on-chain-read bodies are implemented against `@noble/curves` (recoverable secp256k1), `viem` (Sepolia), and `@stellar/stellar-sdk` (Soroban) — the byte-frozen encoding/digest logic is untouched. What was an "honest stub" at the sign/broadcast wall is now a real backend signature followed by a `ChainAdapterNotConfiguredError` deployment gate (funded key + deployed registry address absent), so nothing is ever fabricated. Registry CONTRACTS authored + locally tested (7 Soroban + 7 EVM); live deploy still operator-blocked. Phase 7 (API layer) wired + typechecked + unit-tested + builds — live acceptance BLOCKED.** Phases 1 & 3 complete + verified; Phase 2 schema/client done (live migration still blocked — DATABASE_URL empty); Phase 4 spike delivered; **Phase 5 (Midnight) — the Compact circuit + TS witness provider are now AUTHORED at full scope and LOCALLY TESTED (session 6): the compiled `nivaan.compact` executes in-process under compact-runtime 0.15.0, so `proveComplianceTier` / `issueCredential` (sealed, DID-bound) / `checkNotRevoked` / TTL-expiry logic is proven offline (23 new tests). The LIVE path (deploy to Midnight testnet + real Halo2 proof via a Proof Server) and the backend off-chain verify/issue seams remain blocked (Docker down, `MIDNIGHT_*`/`PROOF_SERVER_URL`/`ANON_AADHAAR_TEST_KEY` empty).** Demo-verifier same-origin proxy route **DONE** (`GET /api/demo-verifier/:id/result`). Frontend / Phase 8 is Antigravity's, not started here.
- Last updated: 2026-09-05
- Overall status: all code-level work through Phase 7 is complete and green — `tsc --noEmit` clean (exit 0, app + the new `contracts/midnight/tsconfig.json` contract layer); **103 unit tests** passing on Vitest 4.1.11 (session 6 added the 23-test Minokawa circuit + witness suite; chains suite is 26); `eslint` 0 errors; `next build` compiles all `/api/*` routes as dynamic handlers. **Both registry contracts have real bodies with local tests passing** (7 Soroban + 7 EVM), each mirroring `lib/chains/attestation.ts` and enforcing the mandatory good-sig-accepted / bad-sig-rejected negative test plus replay guards. The chain adapters now produce a REAL backend signature (independently recovered-to-signer in tests, canonical low-S, v∈{27,28}), then gate the broadcast/read on deployment config. Every boundary that crosses Midnight (proof verify, wallet-signature verify, issuance-circuit eval) still **THROWS a clear "not wired" error — never a fabricated success**; the chain boundary throws `ChainAdapterNotConfiguredError` naming exactly which env var to set. Live/deploy acceptance steps remain externally blocked (empty creds + down toolchain) and are flagged, never faked.
- **Working per the "Two decisions" directive (both APPROVED + now IMPLEMENTED): demo-verifier proxy built; sign/broadcast wired against `@noble/curves` + `viem` + `@stellar/stellar-sdk`. Registry deployment + the funded-key gate remain on the operator. No `app/` (excluding `app/api`) or `lib/wallets/` files edited (Antigravity-owned).**

## Verification snapshot (2026-08-26)
Single batched run, all exit 0:
- `npx prisma generate` → Prisma Client 7.9.1 emitted to `lib/db/generated/prisma` (gitignored). dotenv loaded **13 vars** from `.env.local` via `prisma.config.ts`.
- `npm test` (Vitest 4.1.11) → **3 files / 24 tests passed** (Phase 3 issuance/expiry/revocation/consent transitions).
- `npx tsc --noEmit` → clean: `lib/db/prisma.ts` compiles against the generated client + pg adapter; all 8 route stubs, 4 services, config, and tests typecheck.
- Installed this run: `@prisma/adapter-pg ^7.10.0`, `dotenv ^17.4.2`, `vitest ^4.1.11` (dev).

## Verification snapshot — Phase 5–7 API wiring (2026-08-26, session 2)
All three gates green with NO live services required (DB mocked, external boundaries injected/stubbed):
- `npx tsc --noEmit` → **clean** (all 8 route handlers, 6 services, the `lib/chains` adapter layer, and all tests typecheck).
- `npm test` (Vitest 4.1.11) → **6 files / 54 tests passed** (+30 this session: authService 12, proofService 10, verificationService 8).
- `npx next build` → **compiled successfully**; all 8 `/api/*` routes emitted as dynamic (ƒ) handlers, `/` + `/_not-found` static.
- No packages installed this session — Midnight (`@midnight-ntwrk/*`) and chain-client libs (viem / Stellar SDK) are intentionally deferred to Phases 5/6, where the API is actually wired.
- Env reality (key names + populated status only, values never read): **4 of 13** keys set — the four Phase-0 backend secrets (`BACKEND_ATTESTATION_SIGNING_KEY`, `ISSUER_ADMIN_KEY`, `DEMO_VERIFIER_KEY`, `SESSION_SECRET`). The other **9 are EMPTY**: `DATABASE_URL`, `MIDNIGHT_TESTNET_RPC`, `MIDNIGHT_WALLET_SEED`, `PROOF_SERVER_URL`, `ANON_AADHAAR_TEST_KEY`, `SOROBAN_RPC_URL`, `SOROBAN_TESTNET_SECRET`, `SEPOLIA_RPC_URL`, `SEPOLIA_DEPLOYER_KEY`.

## Preflight + frontend audit — Real-wiring directive (2026-08-27)
Directive: complete Phase 8's real acceptance ("every screen calls its mapped endpoint, handles loading/error/success") + run Phases 9/10 for real. Preflight per Section 0:
- **`.env.local`: unchanged — 4 of 13 set.** All 9 live creds still EMPTY (`DATABASE_URL`, `MIDNIGHT_*`, `PROOF_SERVER_URL`, `ANON_AADHAAR_TEST_KEY`, `SOROBAN_*`, `SEPOLIA_*`).
- **Docker: NOT running. Proof Server (localhost:6300): unreachable** (HTTP 000).
- **Packages: none installed** — no `@midnight-ntwrk/*`, `@anon-aadhaar/core`, `viem`, `@stellar/stellar-sdk`, `ethers`; `package.json` references none.
- **Frontend delivered by Antigravity:** `app/` has hero + 7 screens (`onboarding`, `consent`, `wallet`, `prove`, `result`, `demo-verifier`) + `components/{Hero,Shared}.tsx` + `layout.tsx`. `lib/wallets/` empty (`.gitkeep` only).
- **Conclusion: every "real/live" requirement (Sections 1–4) is blocked at the root** — no DB ⇒ no endpoint can complete (even `/auth/nonce` writes a row); no packages ⇒ no wallet connector and no Test-QR witness; no deployed contracts ⇒ no on-chain verify or negative tests. No screen can be genuinely wired today without fabricating a success state, which the directive forbids. Delivered instead: a code-grounded audit + per-screen wiring spec (see Phase 8 entry) and the flags below. **No frontend files were edited** (Antigravity-owned + design-locked; ownership reassignment flagged).

## Verification snapshot — Phase 6 contracts + `/auth/session` (2026-08-27, session 3)
Working under the "Two decisions" directive (CLAUDE.md ownership split stands; `/auth/session` approved doc-first). All gates run and green:
- **`GET /auth/session`** — added to `docs/api-spec.md` FIRST (frozen-contract rule), then implemented: `app/api/auth/session/route.ts` (thin controller, 401 `UNAUTHENTICATED` when no valid session) reusing `readSessionClaims` → `verifySessionToken`. `SessionClaims`/`verifySessionToken` extended with `expiresAt` (one verification path, no duplicated HMAC logic). Two existing `authService` test assertions updated to include `expiresAt`.
- **`npx tsc --noEmit`** → clean (exit 0). **`npx vitest run`** → **6 files / 54 tests passed** (unchanged count; the two edited assertions still pass).
- **Soroban registry** (`contracts/soroban/`, standalone crate, `soroban-sdk = "25"` per installed Stellar CLI 25.1.0): `cargo test` → **7/7 pass**; `cargo build --target wasm32v1-none --release` → **17K wasm artifact** (dev-deps k256/sha2 correctly excluded from the contract; `secp256k1_recover`/`sha256` are host functions). Migrated event emission to the current `#[contractevent]` macro (no deprecation warnings).
- **Sepolia registry** (`contracts/evm/`, isolated Hardhat package — own `package.json`/`node_modules`, NOT in the Next app): `npx hardhat test` → **7/7 pass**; compiles under solc 0.8.24 / `evmVersion: cancun` (OpenZeppelin 5.6.1 uses the `mcopy` Cancun opcode; Sepolia has run Cancun since Dencun).
- Both contracts mirror `lib/chains/attestation.ts` byte-for-byte and cover the SAME matrix: valid backend-signed attestation ACCEPTED; wrong-signer REJECTED; tampered `result` bit REJECTED (proves `result` is signed); duplicate `credentialId` REJECTED; stale AND future timestamp REJECTED (symmetric 300s window); exact-boundary (300s) ACCEPTED. Signatures are minted with a real secp256k1 key (k256 / ethers), never stubbed.
- **Standalone handoff written:** `docs/frontend-wiring-spec.md` — per-screen method/path/trigger/request/response/error mapping for Antigravity, grounded strictly in `api-spec.md`.
- **Not touched:** no `app/` or `lib/wallets/` file was edited (Antigravity-owned per CLAUDE.md).

## Verification snapshot — wiring spec finalized + adapter encoding bodies (2026-08-27, session 4)
Working under the "Two decisions" directive (both decisions now resolved; see the Phase 8 + flags entries). All gates run and green:
- **`docs/frontend-wiring-spec.md` FINALIZED** (no backend code touched — spec + doc edits only): (1) all four invented UI elements are now **confirmed-for-removal** in the spec (`/wallet` Revoke button + Add-New-Credential card; `/result` Transaction Hash + Verification Payload; `/demo-verifier` Simulate-Verification button + fake logs); (2) the `/consent` screen is **relocated to sit between Wallet and Prove** (not between Onboarding and Wallet) — issuance moved onto `/onboarding` (`POST /credentials/issue`), consent captured on `/consent` and written server-side by `POST /proofs/generate`. Documented as a navigation-order correction (§5), NOT a backend/schema change — `ConsentLog.credentialId` is a required FK, so consent cannot be logged before a credential exists, which forbids the earlier ordering without a schema change we are not making. (3) Confirmed + specified: the demo-verifier's keyed polling **originates server-side through a same-origin proxy** (`GET /api/demo-verifier/:vid/result`) that reads `DEMO_VERIFIER_KEY` from server env and calls `GET /api/verify/:id/result` server-to-server; the key never reaches the browser/bundle/`NEXT_PUBLIC_*`. That proxy route is a pending Claude Code backend deliverable (api-spec-first — see flag below).
- **Chain adapter encoding bodies authored + unit-tested** (`lib/chains/{evm,soroban}.ts`, `attestation.ts`, `errors.ts` — all Claude-Code-owned): the offline, dependency-free, no-funded-key surface each registry contract recomputes is now REAL logic, not a bare stub — `encodeEvmAttestationMessage` (72-byte `abi.encodePacked(bytes32,string,bool,uint256)` preimage), `encodeSorobanAttestationMessage` (48-byte preimage) + `sorobanAttestationDigest` (full SHA-256 digest — SHA-256 is in `node:crypto`), the shared `encodeUintBigEndian`/`resultByte` primitives, and a backend-side freshness guard (`StaleAttestationError`) mirroring each contract's stale/future-timestamp check. The sign + broadcast + on-chain-read boundary stays an **honest stub that THROWS `ChainAdapterNotWiredError`** — never a fabricated tx hash/result.
- **`npx tsc --noEmit`** → clean (exit 0). **`npx vitest run`** → **7 files / 71 tests passed** (+17 this session in `tests/unit/chains.test.ts`; encodings cross-checked with independent hashers, so a regression in the code under test would fail rather than be mirrored). **`npx eslint`** → 0 errors (2 accepted warnings: the two `getResult(_credentialId)` stubs keep the interface-mandated parameter while the read path is genuinely blocked; `_`-prefix is the repo convention).
- **No new dependency added and no security/blockchain assumption changed** — the recoverable-secp256k1 signer + keccak256 (absent from `node:crypto`), the deployed-registry addresses (no env var exists), and the chain-client stack (viem / Stellar SDK) + funded keys are deferred to a developer sign-off, not chosen unilaterally (CLAUDE.md "ask before major changes to … blockchain architecture, security assumptions").
- **Not touched:** no `app/` or `lib/wallets/` file was edited (Antigravity-owned per CLAUDE.md).

## Verification snapshot — chain adapters WIRED + demo-verifier proxy (2026-08-27, session 5)
Both "Two decisions" directives are now IMPLEMENTED (demo-verifier proxy — already done prior session, confirmed on disk; sign/broadcast — done this session). All gates green:
- **`npx tsc --noEmit`** → clean (exit 0). **`npx vitest run`** → **7 files / 80 tests passed** (chains suite 17→26: +9 for the recoverable-signer round-trips and `parseSecp256k1PrivateKey`). **`npx eslint`** (changed files) → 0 errors, 0 warnings (the two `getResult(_credentialId)` stubs are gone — `getResult` now uses its `credentialId` parameter).
- **Dependencies installed (approved in the directive):** `@noble/curves ^2.3.0` (recoverable secp256k1 signing primitive), `viem ^2.56.0` (Sepolia broadcast/read), `@stellar/stellar-sdk ^17.0.1` (Soroban broadcast/read). No blockchain/security assumption changed — these are the sign/broadcast client stack the earlier flag deferred to sign-off; the sign-off is this directive.
- **`lib/chains/signing.ts` (NEW, pure + env-free):** `parseSecp256k1PrivateKey` (validates a 32-byte scalar in [1,n); never echoes the key), `signEvmAttestation` (→ r‖s‖v, v=27+recovery, canonical low-S), `signSorobanAttestation` (→ r‖s‖recovery). Empirically proved against the SAME 0x11..×32 key both contracts' tests use: EVM sig recovers to the signer address; Soroban sig recovers to the 65-byte uncompressed backend pubkey. Unit-tested with INDEPENDENT recovery (viem `recoverAddress` / noble `recoverPublicKey`), so a regression fails rather than mirrors.
- **`lib/chains/evm.ts` + `soroban.ts` WIRED:** `encodeEvmAttestationMessage`/`encodeSorobanAttestationMessage`/`sorobanAttestationDigest` are BYTE-UNCHANGED (directive: "keep the existing encoding/digest logic untouched"). Filled in: sign-FIRST with the ADR-001 root key, then a `missingEnvVars` broadcast/read gate → `ChainAdapterNotConfiguredError` (lists exactly which vars to set), then viem `simulateContract`→`writeContract`→`waitForTransactionReceipt` (EVM) / Stellar `prepareTransaction`→`sendTransaction`→poll `getTransaction` (Soroban). The Stellar SDK is imported LAZILY (dynamic `import()` after the gate) so unit tests never load it. `getResult` reads via `hasResult`+`getResult` (EVM) / simulate `get_result` → `scValToNative` (Soroban), returning `null` for no-attestation.
- **`lib/chains/errors.ts`:** `ChainAdapterNotWiredError` retained for a genuinely-unwired FUTURE boundary; NEW `ChainAdapterNotConfiguredError` (code exists, deployment config absent — carries `missing` var NAMES only) and `InvalidSigningKeyError` (malformed root key; never echoes the value).
- **`lib/config/env.ts` + `.env.example` + `docs/roadmap.md`:** added two required env vars — `SOROBAN_REGISTRY_CONTRACT_ID` and `SEPOLIA_REGISTRY_ADDRESS` (the deployed-registry addresses recorded after the CLI deploy, architecture.md §12), with lazy getters. NEW `missingEnvVars(names)` helper (returns absent NAMES, never throws, never reads a value) powers the deployment gate. `REQUIRED_ENV_VARS` is now 15 entries.
- **Demo-verifier proxy confirmed DONE** on disk: `app/api/demo-verifier/[id]/result/route.ts` — keyless from the client, reads `DEMO_VERIFIER_KEY` server-side purely as an enablement gate, calls `verificationService.getVerificationResult` DIRECTLY (no self-HTTP, key never transmitted), 404 on unknown id. Matches the frozen `docs/api-spec.md` entry.
- **`npm audit` — 3 high-severity, NOT auto-fixed (flagged for operator, see below):** all three trace to one transitive dep, `deepmerge-ts@7.1.5` (GHSA-ggr8-5vv4-36mx, stack exhaustion on recursive object graphs), pulled in by `@prisma/config` → `prisma`. No non-breaking fix exists — even the latest Prisma 7 (`@prisma/config@7.10.0`) still pins `deepmerge-ts@7.1.5`; `npm audit fix --force` would downgrade to `prisma@6.12.0` (backward breaking) and the only forward path is `prisma@8.0.0-rc` (unstable RC). Left unchanged: Prisma is database technology (CLAUDE.md requires asking first) and the directive says don't `--force` blindly.
- **Still operator-blocked (unchanged):** funded `SOROBAN_TESTNET_SECRET`/`SEPOLIA_DEPLOYER_KEY`, the deployed `SOROBAN_REGISTRY_CONTRACT_ID`/`SEPOLIA_REGISTRY_ADDRESS`, and both RPC URLs. Until those are set, `submitAttestation` signs then throws `ChainAdapterNotConfiguredError`; on-chain acceptance is the remaining deploy-gated step.
- **Not touched:** no `app/` (excluding `app/api`, which is Claude Code's) or `lib/wallets/` file was edited.

## Verification snapshot — Phase 5 Minokawa circuit + witness (2026-09-05, session 6)
The Midnight credential circuit and its TypeScript witness are now AUTHORED at full scope and proven at the LOGIC level offline — `@midnight-ntwrk/compact-runtime` executes the compiled circuit in-process, so NO Proof Server / testnet / midnight-js is required for the logic tests (local execution proves logic; proof GENERATION + on-chain submission is the separately-blocked live step). All gates green:
- **`contracts/midnight/nivaan.compact`** — authored + compiles clean (Compact 0.5.1; compiler 0.30.0 / language 0.22.0 / runtime 0.15.0). Circuits: `tick()` (epoch Counter), `proveComplianceTier(threshold): Boolean` (discloses ONLY age≥threshold, no ledger write), `issueCredential(did, threshold, ttlEpochs): Boolean` (asserts the DID is unseen → inserts a **sealed, DID-bound** `CredentialRecord` disclosing only {eligible, jurisdictionCode, ttlEpochs, issuedAtEpoch=epoch}, bumps `issuedCount`), `revoke(did)`, `checkNotRevoked(did): Boolean` (false if absent/revoked/ineligible/expired, else live). Ledger: `credentials` Map, `revoked` Map, `epoch` Counter, `issuedCount` Counter — **TTL via Counter** per architecture.md §5/§7.
- **`contracts/midnight/witnesses.ts` (NEW)** — the TS side of `witness getAadhaarTestProof(): AadhaarAttrs`. Reads the Anon Aadhaar Secure QR V2 payload from the contract's PRIVATE STATE (client-side, never sent to the backend — §2/§6/security-model.md) and derives {ageYears, jurisdictionCode (coarse 2-digit PIN region — deliberately not location-pinpointing since it is disclose()'d), nullifier = sha256(photo) via `node:crypto`}. Malformed QR → `InvalidAadhaarQrError` (THROWS, never fabricates an eligible attribute). Data-source-agnostic (ADR-003): the post-MVP REAL_DATA swap changes only `extractAadhaarAttrs`, not the circuit / ledger / backend.
- **Packages installed (Phase-5 wiring):** `@midnight-ntwrk/compact-runtime@0.15.0` (local circuit execution) + `@anon-aadhaar/core@2.4.3` (Test-QR decode). The Midnight proof/deploy stack (`@midnight-ntwrk/midnight-js-*`) is NOT installed yet — that lands with the blocked live step.
- **`tests/unit/nivaanCircuit.test.ts` (NEW) — 23 tests.** (1) Circuit logic with a controllable witness — valid (adult, age≥threshold → eligible + sealed ledger record + issuedCount) AND invalid (minor → not eligible) per security-model's "cover valid + invalid witness inputs"; double-issue rejected (sealed); revocation; Counter-relative TTL expiry via `tick`. (2) Witness extraction — age from DD-MM-YYYY, coarse jurisdiction, stable-per-identity nullifier (different photo → different nullifier), throws on malformed QR. (3) End-to-end: synthetic Test QR → `createNivaanWitnesses` → circuit → eligible/ineligible.
- **`npx vitest run`** → **8 files / 103 tests passed** (+23; the prior 80 unchanged). **`npx tsc --noEmit`** (app) → clean (exit 0). **`npx tsc -p contracts/midnight/tsconfig.json`** (contract layer) → clean (exit 0). **`npx eslint`** (new files) → 0.
- **Toolchain note — why a second tsconfig:** `@anon-aadhaar/core` publishes `"types": "./src/index.ts"` with no compiled `.d.ts`, so TypeScript type-checks the library's non-compiling raw source (untyped uuid/pako/json-bigint; ArrayBuffer casts in prover.ts) and `skipLibCheck` cannot help (it only skips `.d.ts`). Fix: the Midnight TS layer (witness + its test) is a SEPARATE build unit — `contracts/midnight/tsconfig.json` with a scoped `paths` shim (`contracts/midnight/types/anon-aadhaar-core.d.ts`, **type-only, never a runtime alias** — Node/Vitest resolve the real module) — and the root tsconfig now excludes it, so `next build` never drags the contract toolchain into the app's type program. New scripts: `npm run typecheck` (app) + `npm run typecheck:contracts`.
- **Sealing semantics (docs silent → documented actual behavior):** §5/§7 fix the flow (proveComplianceTier gate → issueCredential writes the sealed DID-bound entry, discloses only a boolean, TTL via Counter) but do not say whether an *ineligible* issue attempt should seal the DID. The compiled contract seals on ANY attempt (the uniqueness assert precedes the eligibility branch); an ineligible attempt writes `eligible=false` and returns false, so the backend's `circuitApproved` gate still refuses the DB credential (`CredentialCriteriaNotMetError`). Tests encode this ACTUAL behavior. If "insert only when eligible" (retry-after-18) is later required, that's a one-line circuit change + recompile.
- **Acceptance still BLOCKED (root cause unchanged):** the LIVE Phase-5 step — deploy the compiled contract to Midnight testnet + generate/verify a real Halo2 proof via a running Proof Server, then wire the backend `verifyProof`/`evaluateIssuanceCircuit` seams to midnight-js — needs `MIDNIGHT_TESTNET_RPC`/`MIDNIGHT_WALLET_SEED`/`PROOF_SERVER_URL`/`ANON_AADHAAR_TEST_KEY` (all EMPTY) + Docker up. The circuit LOGIC is now real and locally proven; the on-chain/proof acceptance is the remaining, resource-gated step.

## Phase History

_Blueprint (7 source-of-truth docs + CLAUDE.md) is on disk and passed a cross-doc consistency read (blockers B1–B4, now resolved). Nothing committed to git yet._

### Phase 0 — Setup
- Status: **code/local work complete; acceptance deploys blocked on human/external setup.**
- Scaffold (2026-08-23): `create-next-app` (TS, Tailwind, App Router, ESLint) at repo root; docs/CLAUDE.md/.env.example/.gitignore preserved. Next 16.3.1, React 19.2.8. Directory tree per `architecture.md`.
- Toolchain inventory (read-only, 2026-08-19): Node v22.23.1 / npm 10.9.8 / pnpm 11.13.0 ✓; Rust 1.93.1 + cargo ✓; Stellar CLI 25.1.0 ✓; Hardhat 3.0.10 ✓; Compact compiler 0.5.1 ✓; Docker 29.5.3 installed but daemon NOT running; git 2.51.0 ✓.
- Secrets: four backend-generated secrets (`BACKEND_ATTESTATION_SIGNING_KEY`, `SESSION_SECRET`, `ISSUER_ADMIN_KEY`, `DEMO_VERIFIER_KEY`) generated into `.env.local` (mode 600, gitignored), values never printed.
- Acceptance (trivial deploys on all three testnets + `next dev`, same sitting): **BLOCKED** on Docker + funded keys + external accounts (human/external setup).

### Phase 1 — Core backend scaffold — **COMPLETE ✅**
- `lib/config/env.ts` — typed single-source env accessor; lazy getters (import never throws on a missing var), name-only errors (never logs values); `REQUIRED_ENV_VARS`, `presentEnvVars()`, `assertRequiredEnv()`.
- `lib/api/http.ts` — shared `ApiError` + `notImplemented()` (`501` + `{error,code}` + `X-Nivaan-Stub`). Stubs return NO fabricated success bodies, so an unwired endpoint can't read as working.
- **8** API route stubs, matching `api-spec.md` exactly, each with inline request/response types.
- `lib/db/prisma.ts` — **DONE + verified.** Prisma 7 pg driver-adapter (`PrismaPg`) singleton; imports the generated client from `./generated/prisma/client`; `globalThis` cache in dev; re-exports generated model types + enums so services import them from `@/lib/db/prisma`.
- `prisma.config.ts` — loads `.env.local` via dotenv; `datasource.url = process.env.DATABASE_URL` (undefined-safe: `generate`/`validate` succeed even before a DB is reachable). `schema.prisma` generator switched to the v7 default `prisma-client` (output `./generated/prisma`, gitignored).
- Acceptance ("endpoint list and route file tree match exactly" + typed stub responses): **MET** (tsc clean; tree ↔ api-spec one-to-one).

### Phase 2 — Database — **schema/client done; live insert/query pending**
- `lib/db/schema.prisma`: all six models (AuthNonce, Credential, RevokedCredential, ConsentLog, ProofRequest, VerificationResult) + three enums (CredentialStatus, ChainTarget, ProofStatus), transcribed exactly from `data-model.md` — no extra or PII-shaped columns. `prisma validate` + `prisma generate` both pass.
- Acceptance: schema-match + no-PII + client-generation portions **MET**. "Manual insert/query against each table succeeds" is **BLOCKED** — a names-only `.env.local` check (2026-08-26, session 2) confirms `DATABASE_URL` is EMPTY, so `prisma migrate` + a per-table insert/query cannot run yet. Unblocks the moment `DATABASE_URL` is populated and the DB is reachable.

### Phase 3 — Credential system — **COMPLETE ✅**
- `lib/config/policy.ts` — B4 constants: `CREDENTIAL_TIER='kyc_tier_1'`, `SUPPORTED_JURISDICTION='IN'`, `CREDENTIAL_TTL_DAYS=30`, `MVP_POLICY_ID='kyc_tier_1'`.
- `lib/services/credentialService.ts`:
  - `deriveEffectiveStatus(credential, isRevoked, now)` — pure; revocation > expiry precedence; `expiresAt <= now` ⇒ EXPIRED; never trusts a stale stored ACTIVE (per `data-model.md`/`security-model.md`).
  - `issueCredential()` — jurisdiction guard (→ `UnsupportedJurisdictionError`), circuit-approval guard (→ `CredentialCriteriaNotMetError`), writes ACTIVE / `kyc_tier_1` / 30-day TTL. DB-only (not Midnight) per roadmap Phase 3.
  - `getCredentialStatus()` — 404 (`CredentialNotFoundError`) / 403 (`NotCredentialOwnerError`); returns COMPUTED status.
- `lib/services/revocationService.ts` — `revokeCredential()` re-checks `ISSUER_ADMIN_KEY` (backend-authoritative; never logged), records `RevokedCredential` + flips status in one `$transaction`; `isRevoked()`. Dev-only issuer op, no frontend route (ADR-005).
- `lib/services/consentService.ts` — SHA-256-hex hashing mechanism + known-hash gating + `logConsent()`. ⚠️ see Consent flag.
- Tests: `tests/unit/{credentialService,revocationService,consentService}.test.ts` — **24 tests, all pass.** Mock `@/lib/db/prisma` (and `@/lib/config/env`) via `vi.hoisted`/`vi.mock`, so they need neither the generated client nor a live DB.
- Acceptance ("unit tests pass for issuance/expiry/revocation state transitions using mock circuit output"): **MET.**

### Phase 4 — ZK / privacy-layer research validation — **spike delivered; acceptance BLOCKED**
- Deliverable (spike: which `midnight-js` function verifies a proof server-side) — delivered, but **low confidence**: the research agent's network access was blocked for its whole window, so most findings are `[INFERENCE]`/`[UNCONFIRMED]`. Treated as leads to confirm against real `.d.ts`, not ground truth.
- Verified (npm registry): scope is `@midnight-ntwrk/*` (not "midnight-js"/"Minokawa" as npm names); `@midnight-ntwrk/midnight-js-*` family at **4.1.1**; `@midnight-ntwrk/ledger` latest 4.0.0 with parallel `ledger-vN`; compiler is `compactc` (Compact 0.5.1 installed locally per Phase 0).
- Leads (unconfirmed): verifier-key handling in Node via `@midnight-ntwrk/midnight-js-node-zk-config-provider` (`NodeZkConfigProvider.getVerifierKey`); the actual verify likely a `@midnight-ntwrk/ledger` WASM transaction well-formedness check (offline, no Proof Server needed), NOT a bare `verifyProof()`. Verifier key emitted by `compactc` into a `managed/<contract>/keys/*.verifier` layout. Must set `setNetworkId(TestNet)` before using ledger/runtime. No confirmed dedicated CLI verify command — budget for a small Node script.
- **Acceptance ("verify a sample proof from the CLI") — BLOCKED:** needs the Midnight toolchain operational — no `@midnight-ntwrk/*` installed yet, Docker/Proof Server down, and no compiled circuit ⇒ no verifier key and no sample proof to verify. Confirmation steps recorded for a Bash+network-up re-run (fetch `docs.midnight.network/llms.txt`, read `/api-reference` typedoc, `npm pack @midnight-ntwrk/ledger` and grep the `.d.ts`). Midnight packages are intentionally NOT installed yet; install lands with Phase 5 (where the API is actually wired).

(append new phase entries chronologically, newest at the bottom)

### Phase 5 — Midnight integration — **circuit + witness AUTHORED + LOCALLY TESTED (session 6); live proof/deploy + backend verify seams BLOCKED**
- **The Compact circuit + TS witness are now real (not a stub) and proven offline** — see the session-6 verification snapshot above. `contracts/midnight/nivaan.compact` compiles clean and executes in-process under `@midnight-ntwrk/compact-runtime@0.15.0`; `contracts/midnight/witnesses.ts` implements `getAadhaarTestProof` against `@anon-aadhaar/core@2.4.3` Test QRs; `tests/unit/nivaanCircuit.test.ts` (23 tests) proves `proveComplianceTier` / `issueCredential` (sealed, DID-bound) / `checkNotRevoked` / TTL-expiry + witness extraction, covering valid AND invalid witness inputs. This is the credential-forgery control (security-model.md) exercised at the logic level with NO Proof Server required (local execution proves logic; proof GENERATION/submission is the separate blocked step).
- **The three backend Midnight-crossing seams still THROW rather than fabricate** — the LIVE off-chain verify/issue path is what's blocked, not the circuit:
  - `proofService.verifyProof()` → `MidnightVerificationUnavailableError` (server-side proof verification via midnight-js — Phase 4 lead).
  - `credentialService.evaluateIssuanceCircuit()` → `MidnightUnavailableError` (issuance-circuit verdict — the seam the wired circuit will feed once the proof stack is up; distinct from the *reached* verdict `CredentialCriteriaNotMetError`/422).
  - `authService` default wallet-signature verifier → `WalletSignatureVerificationUnavailableError` (ADR-004 wallet/Anon-Aadhaar check). Injected dep — real impl throws; tests inject a mock.
- Acceptance ("verify a sample proof"; issue against Midnight testnet): **BLOCKED on the live stack** — generating/verifying a real Halo2 proof needs a running Proof Server (Docker down) + `MIDNIGHT_*`/`PROOF_SERVER_URL`/`ANON_AADHAAR_TEST_KEY` (all EMPTY), and deploying the compiled contract needs the Midnight testnet wallet/RPC. The circuit itself is authored, compiled, and locally test-passing; only the resource-gated on-chain/proof step remains.

### Phase 6 — Cross-chain registries + adapters — **contracts authored + LOCALLY TESTED; adapter bodies + live deploy PENDING**
- `lib/chains/` authored and typechecked:
  - `attestation.ts` — the authoritative signing scheme + canonical field layout: secp256k1 on BOTH chains from the single `BACKEND_ATTESTATION_SIGNING_KEY` (ADR-001 root of trust); per-chain digest (EVM keccak256 + eth-signed-message prefix, Soroban SHA-256); fixed-width ordered fields `{credentialId32, chainTag("SOROBAN"|"SEPOLIA"), resultByte, timestamp}`; `ATTESTATION_MAX_AGE_SECONDS=300` replay/staleness window; `credentialIdToBytes32`, `isTimestampFresh`, `nowUnixSeconds`.
  - `types.ts` — one `ChainAdapter` interface (`submitAttestation`/`getResult`) so verificationService stays chain-agnostic; `AttestationFields`, `SubmitAttestationResult` (returns the submitted signature for the `VerificationResult.attestationSig` audit trail).
  - `index.ts` — `getChainAdapter` registry (exhaustive `never`-checked switch) + `parseChainTarget`/`chainToApiString` api-spec wire mapping.
  - `evm.ts` + `soroban.ts` — implement ChainAdapter FULLY (session 5). The offline encoding each contract recomputes is BYTE-UNCHANGED: `encodeEvmAttestationMessage` (72-byte `abi.encodePacked` preimage), `encodeSorobanAttestationMessage` (48-byte preimage) + `sorobanAttestationDigest` (full SHA-256 digest), plus a `StaleAttestationError` freshness guard run first. NOW WIRED: the sign step (`lib/chains/signing.ts` via `@noble/curves`), the broadcast (viem for Sepolia, `@stellar/stellar-sdk` for Soroban), and the on-chain read (`hasResult`+`getResult` / simulate `get_result`). The old sign/broadcast wall is replaced by a **`ChainAdapterNotConfiguredError` deployment gate** thrown AFTER the signature is computed, so a missing funded key / registry address is reported precisely (by var name) and a result is never fabricated. `errors.ts` adds `ChainAdapterNotConfiguredError`/`InvalidSigningKeyError` (and retains `StaleAttestationError`); `attestation.ts` has `encodeUintBigEndian`/`resultByte`. Covered by `tests/unit/chains.test.ts` (26 tests, incl. independent recover-to-signer round-trips).
- **Contracts NOW authored + locally tested (2026-08-27, session 3):**
  - `contracts/soroban/` — standalone crate (`soroban-sdk = "25"`). `Registry` with `init(backend_pubkey: BytesN<65>)`, `submit_attestation(credential_id: BytesN<32>, result, signature: BytesN<65>, timestamp: u64)`, `get_result -> Option<VerificationResult>`, `backend_key()`; digest = `sha256(credentialId ‖ "SOROBAN" ‖ resultByte ‖ u64_be(timestamp))`, `secp256k1_recover`, recovered key must equal configured backend key; rejects duplicate `credentialId` + timestamp outside ±300s; emits an `attest` `#[contractevent]`. `cargo test` **7/7**; release build to `wasm32v1-none` (soroban-sdk 25 rejects `wasm32-unknown-unknown` on Rust ≥1.82) → 17K wasm.
  - `contracts/evm/` — isolated Hardhat package (own `package.json`/`node_modules`; NOT in the Next app). `Registry.sol` (pragma ^0.8.24, OZ 5.6.1 `ECDSA` + `MessageHashUtils`): immutable `backendSigner`, `submitAttestation(bytes32,bool,bytes,uint256)`, `getResult(bytes32) view returns (bool,uint256)` + additive `hasResult`; digest = `keccak256(abi.encodePacked(credentialId,"SEPOLIA",result,timestamp))` → eth-signed prefix → `ECDSA.recover == backendSigner`; rejects duplicate + ±300s; custom errors `AlreadyRecorded`/`StaleTimestamp`/`InvalidSignature`. `npx hardhat test` **7/7** (solc 0.8.24, `evmVersion: cancun`).
  - **Mandatory Phase-6 negative test — WRITTEN + PASSING (local)** on both chains: good backend-signed attestation accepted, wrong-signer rejected; plus tampered-result, duplicate, stale, future, and exact-boundary cases. Signatures minted with a real secp256k1 key (k256 / ethers), not stubbed.
- **Sign + broadcast + read — ✅ DONE (session 5):** the recoverable-secp256k1 signer (`@noble/curves`), keccak256/eth-prefix (viem), the Sepolia broadcast/read (viem) and the Soroban broadcast/read (`@stellar/stellar-sdk`) are all wired; the two deployed-registry env vars now exist (`SOROBAN_REGISTRY_CONTRACT_ID`, `SEPOLIA_REGISTRY_ADDRESS`). **Still operator-blocked for LIVE deploy:** funded `SOROBAN_TESTNET_SECRET`/`SEPOLIA_DEPLOYER_KEY`, the two registry addresses' actual values (deploy the contracts first), and the RPC URLs — until set, `submitAttestation` signs then throws `ChainAdapterNotConfiguredError` naming the missing vars.
- Acceptance (deploy to Soroban + Sepolia testnets; accept good / reject bad signature **on-chain**): **BLOCKED** on funded testnet keys (`SOROBAN_*`/`SEPOLIA_*` all EMPTY). Contract bodies + the negative test are now DONE and green at the local unit level; the on-chain acceptance is the remaining, deploy-gated step.

### Phase 7 — API layer — **CODE COMPLETE + green; live acceptance BLOCKED**
- All **8** route handlers wired from stubs to their services, matching docs/api-spec.md contracts (status codes, `{error,code}` envelope, documented side effects). Verified by re-reading every route file this session:
  - `GET /api/auth/nonce` → `issueNonce` → 200; 500 `NONCE_GENERATION_FAILED`.
  - `POST /api/auth/verify` → `verifyAndCreateSession`; on success sets **HttpOnly + Secure + SameSite=Strict** session cookie (maxAge `SESSION_TTL_SECONDS`). 200; 400 `INVALID_SIGNATURE`/`INVALID_JSON`/`INVALID_REQUEST`, 401 `NONCE_UNKNOWN`/`NONCE_EXPIRED`, 409 `NONCE_ALREADY_USED`, 500 `SIGNATURE_VERIFICATION_UNAVAILABLE`.
  - `POST /api/credentials/issue` → session-gated; `evaluateIssuanceCircuit` → `issueCredential`. 201; 400 `UNSUPPORTED_JURISDICTION`/`INVALID_REQUEST`, 422 `CRITERIA_NOT_MET`, **500 `MIDNIGHT_UNREACHABLE` at runtime today** (issuance-circuit seam throws — honest, never fabricates approval).
  - `GET /api/credentials/:id/status` → `getCredentialStatus` (async route param awaited); 200 (status lowercased to api wire value); 403 `NOT_CREDENTIAL_OWNER`, 404 `CREDENTIAL_NOT_FOUND`.
  - `POST /api/proofs/generate` → consent-gated `createProofRequest`; 202; 400 `INVALID_CHAIN`/`INVALID_POLICY`/`UNKNOWN_CONSENT_HASH`/`INVALID_REQUEST`, 403/404, 409 `CREDENTIAL_NOT_ACTIVE`.
  - `GET /api/proofs/:id/status` → `getProofStatus`; 200; 403/404.
  - `POST /api/verify` → `verifyAndAttest`; 202; 403/404, 409 `PROOF_NOT_READY`, 500 `VERIFICATION_FAILED` (covers the unwired Midnight-verify + chain-adapter boundaries honestly).
  - `GET /api/verify/:id/result` → **dual auth** (session credential-owner OR constant-time `DEMO_VERIFIER_KEY` header via `timingSafeEqual`) → `getVerificationResult`; 200; 401 unauth, 403 not-owner, 404 `VERIFICATION_NOT_FOUND`.
- Backend-authoritative authorization: ownership is enforced INSIDE the services, never trusting route/client input. `verifyAndAttest` now loads the credential and checks `credential.ownerWallet` **before** disclosing or writing anything (added this session). Unknown errors → generic 500 `INTERNAL_ERROR` with **no raw-error logging** (avoids leaking secrets embedded in Prisma/connection errors).
- Tests: `tests/unit/{authService,proofService,verificationService}.test.ts` — **30 new tests**; DB mocked via `vi.hoisted`/`vi.mock`, external boundaries injected, `node:crypto` real. Assert validation ordering, idempotency (existing `VerificationResult` returned without re-submitting), the attestation write (persisted `attestationSig`), the session-token round-trip/tamper/expiry, and that every unwired boundary THROWS (never a faked pass).
- Acceptance ("a full request/response cycle for every endpoint, tested via a REST client, matches the documented contract exactly" against "the real DB, Midnight, and both chains"): **BLOCKED** — requires Phase 2 live DB (`DATABASE_URL` EMPTY), Phase 5 Midnight (toolchain down), and Phase 6 deployed registries (not authored/deployed). The CODE is complete, typechecked, unit-tested, and builds; **Phase 7 is NOT marked complete** against its live-acceptance criterion.

### Phase 8 — Frontend integration — **visual shell delivered by Antigravity; functional wiring BLOCKED; NOT complete**
- Antigravity delivered the visual shell for all surfaces (reusing locked tokens). Roadmap acceptance — "every screen successfully calls its mapped endpoint and handles loading/error/success states" — is **NOT met**. A code-grounded audit (2026-08-27, read-only) confirms every screen is static/simulated:
  - **Login state** is a hardcoded `const isLoggedIn` per page (`Hero` carries `// TODO: wire to actual auth state`), threaded to `Nav`/`PageContainer`. No session, no real `/auth/*` call.
  - **`/onboarding`:** buttons inert; "Continue to Consent" is a `<Link>`. No `@anon-aadhaar/core` witness, no `POST /credentials/issue`.
  - **`/consent`:** "Approve & Issue" is a `<Link href="/wallet">`; no `consentHash` computed, no backend write.
  - **`/wallet`:** static card (invented "Verified Identity" / "Midnight Network" / "Oct 24, 2026"→"Oct 24, 2027"); no `GET /credentials/:id/status`. Contains an **"Add New Credential"** dashed card (no api-spec mapping — flagged for removal) and a **"Revoke"** button (contradicts ADR-005: revocation is a dev-only admin script, not a frontend action — flagged for removal).
  - **`/prove`:** `handleGenerate` is a `setInterval` fake progress bar (+10 / 400ms) → `window.location.href='/result'`. No `POST /proofs/generate`, no `GET /proofs/:id/status` poll. `selectedNetwork` state is never sent.
  - **`/result`:** hardcoded success + copy pinned to "the Soroban network"; displays a **"Transaction Hash"** (`0x8f2a…39d1b`) and a **"Verification Payload"** JSON (`groth16`/`bn128`/`pi_a…`) — **neither field is in the `GET /verify/:id/result` contract** (`{verificationId, chain, result, verifiedAt}`). Invented data; flagged for removal.
  - **`/demo-verifier`:** "Mock Service Partner App" with a **"Simulate Verification"** button that flips local state + hardcoded fake Soroban logs (contract `0x8A4b…`, "Proof received! Hash: 0x8f2a…39d1b", Groth16 parsing). No real `GET /verify/:id/result` poll with the `DEMO_VERIFIER_KEY` header.
- Real wiring is blocked on: live DB (all 8 endpoints — even `/auth/nonce` writes a row), `@midnight-ntwrk/*` + RPC + a wallet (session + connector), `@anon-aadhaar/core` + key (onboarding witness), Midnight/Proof Server (issuance/proof), deployed registries (verify). **Not started by Claude Code:** editing these files is Antigravity's domain per CLAUDE.md and design-locked — ownership reassignment flagged below. Delivered instead: this audit + a per-screen wiring spec handed to the operator.
- **`docs/frontend-wiring-spec.md` FINALIZED (2026-08-27, session 4) — Antigravity may now build against it, no further changes pending on the backend side.** It resolves the audit's flags: (1) the four invented UI elements above are **confirmed-for-removal** (Revoke button + Add-New-Credential card + Transaction Hash + Verification Payload + Simulate-Verification button + fake logs); (2) navigation order corrected — `/consent` now sits **between Wallet and Prove**, issuance fires on `/onboarding`, consent is written by `POST /proofs/generate` (a frontend navigation-order fix, NOT a backend/schema change — pinned by `ConsentLog.credentialId`'s required FK); (3) the demo-verifier's keyed poll goes through a **same-origin server-side proxy** so `DEMO_VERIFIER_KEY` never reaches the client (that proxy is a pending Claude Code deliverable — see flag below).

### Phase 9 — End-to-end integration — **BLOCKED (cannot run)**
- Requires the full live stack (wallet connect → onboarding with real Test QR → consent → issue on Midnight testnet → proof via real Proof Server → verify on real Soroban then Sepolia → result shows pass for both). None of that stack exists yet. Not attemptable; **not complete**.

### Phase 10 — Negative tests against real deployed contracts — **BLOCKED (cannot run)**
- Requires deployed Soroban + Sepolia registries to reject an expired credential AND a badly-signed attestation **on-chain** (not merely in a local test). The bad-signature (and duplicate / stale / tampered / boundary) negative tests are now **WRITTEN and PASSING locally** against both compiled contracts (Phase 6, session 3) — but the contracts are **not deployed** (`SOROBAN_*`/`SEPOLIA_*` EMPTY), so the on-chain rejection cannot be exercised yet. The expired-credential rejection path is a backend/issuance concern (Midnight-blocked). **Not complete.**

## Testing infrastructure
- Runner: **Vitest 4.1.11** (chosen within `architecture.md`'s "Vitest/Jest" latitude — ESM-native, matches Prisma 7 + Next 16; first-class module mocking). Scripts: `npm test` (`vitest run`), `npm run test:watch`.
- `vitest.config.mts`: node env, `include: tests/**/*.test.ts`, `@` alias → `process.cwd()` (mirrors tsconfig `@/* → ./*`).
- ✅ Resolved (session 2): the config is now `vitest.config.mts` (renamed from `.ts`), so the earlier ESM-in-CJS warning is gone; `npm test` runs clean.

## Resolved Decisions (pre-build blockers B1–B4)
- [x] **B1 — Table count.** Six tables (Prisma schema authoritative); stale "five" corrected across docs.
- [x] **B2 — Consent capture.** On `POST /proofs/generate`; `consentHash` in body + validation + `400 …/consent` + `consent_logs` write.
- [x] **B3 — Revocation invocation.** Dev-only admin script authorized by `ISSUER_ADMIN_KEY`, not a frontend API (ADR-005).
- [x] **B4 — policyId semantics.** Single hardcoded MVP policy id in `lib/config` (`policy.ts`) mapping to the `kyc_tier_1` check.

## Open flags & blockers (per master-prompt Section 5)
- **⚠️ Consent text/hash contract (Phase 3, needs Antigravity coordination).** The consent TEXT shown to the user and the canonicalization deriving `consentHash` are a shared frontend↔backend contract; `data-model.md` says the text is "static and versioned in code" but specifies neither copy nor algorithm. Provisional backend decision: SHA-256 over exact UTF-8 text bytes, hex; `CONSENT_TEXT_VERSIONS.v1` is PLACEHOLDER copy. Frontend must compute the hash identically. Tests pin the mechanism, not the copy, so finalizing the text won't require rewriting them.
- **⚠️ Attestation signing scheme (Phase 6 — NOW AUTHORED as documented default).** Implemented in `lib/chains/attestation.ts`: secp256k1 on both chains from the single backend key; EVM keccak256 + eth-signed-message prefix, Soroban SHA-256; fixed-width field layout `{credentialId32, chainTag, resultByte, timestamp}`; 300s replay/staleness window. This is a documented default under ADR-001, NOT an independently specified requirement — `architecture.md §9` only pins the EVM `ECDSA` side; the Soroban curve + the shared message encoding were unspecified. If overridden before live deploy, change it in exactly three places: `attestation.ts`, `Registry.sol`, `lib.rs`. **Both registry contracts now MIRROR this and are locally tested** (session 3) — a tampered-`result` test on each proves the field layout is actually signed. **The adapters (`evm.ts`/`soroban.ts`) are now FULLY WIRED (session 5)**: signing via `@noble/curves` (recoverable secp256k1; keccak256/eth-prefix via viem), broadcast/read via viem (Sepolia) and `@stellar/stellar-sdk` (Soroban), proven correct by independent recover-to-signer round-trip tests. What remains is purely operational — deploy the two registries and provision funded keys + the `SOROBAN_REGISTRY_CONTRACT_ID`/`SEPOLIA_REGISTRY_ADDRESS`/RPC vars — gated by `ChainAdapterNotConfiguredError`, not by any missing code. The scheme itself is still a documented default: if overridden before live deploy, change it in exactly three places: `attestation.ts`, `Registry.sol`, `lib.rs`.
- **⚠️ authService seams + defaults (Phase 5/7).** Three Section-5 decisions, referenced by `Flag 1`/`Flag 2` comments in code: (1) **Nonces are issued UNBOUND** (`walletAddress=''`) and bound to the wallet at verify time (`update … {used:true, walletAddress}`) — api-spec's `GET /auth/nonce` takes no wallet, so binding-at-verify is the documented default. (2) **Wallet-signature verification is an unwired injected seam** — the default impl throws `WalletSignatureVerificationUnavailableError` (→500), never fakes a pass; ADR-004's real check lands with the Midnight/wallet libs. (3) **DID default `did:nivaan:<walletAddress>`** — `data-model.md` requires a wallet-derived unique DID but doesn't pin the format; this is the documented default.
- **⚠️ Issuance-circuit seam (Phase 5/7).** `credentials/issue` runs session-auth + input validation, then `evaluateIssuanceCircuit` — which throws `MidnightUnavailableError` until Midnight is wired, so the endpoint returns **500 `MIDNIGHT_UNREACHABLE` at runtime today**. Honest by design: issuance cannot complete without a real circuit verdict, and approval is never fabricated.
- **⚠️ Demo-verifier header name + 425 deviation (Phase 7).** `GET /verify/:id/result` reads the `DEMO_VERIFIER_KEY` header verbatim from api-spec's wording (HTTP header lookup is case-insensitive) and compares it constant-time. The api-spec's **425 ("too early") is unreachable** in the synchronous MVP — a `verificationId` only exists once its result row is written — so an unknown id is a 404, not a 425. Confirm the exact header name with the api-spec/Antigravity before demo.
- **✅ RESOLVED (2026-08-27, session 5) — Demo-verifier same-origin proxy route (Phase 7/8).** `docs/frontend-wiring-spec.md` (final) requires the demo-verifier's keyed polling to originate server-side so `DEMO_VERIFIER_KEY` never enters the client bundle or a `NEXT_PUBLIC_*` var (security-model.md: it is a secret). Built per the frozen-contract rule: added to `docs/api-spec.md` FIRST (`GET /api/demo-verifier/:id/result`, keyless from the client), then implemented `app/api/demo-verifier/[id]/result/route.ts`. The route takes **no key from the request** — it reads `DEMO_VERIFIER_KEY` from its OWN server env purely as an enablement gate (`void env.demoVerifierKey` inside try/catch → 500 if the deployment forgot to set it) and calls the same `getVerificationResult(id)` service function directly (no self-HTTP hop). Response `200 { verificationId, chain, result, verifiedAt }`; `404` unknown id (`VerificationResultNotFoundError`); the api-spec's `425` "still pending" is documented but unreachable in the synchronous MVP (a `verificationId` only exists once its result row is written). The browser now sends ONLY the `verificationId`; the secret stays server-side.
- **⚠️ Resume-directive credential claim did NOT match reality (Section 5).** The standing directive listed `DATABASE_URL`, `MIDNIGHT_WALLET_SEED`, `MIDNIGHT_TESTNET_RPC`, `SEPOLIA_RPC_URL`, `SEPOLIA_DEPLOYER_KEY` as "now available" — a `.env.local` check (names + populated status only) shows **all five are EMPTY**. Only the 4 Phase-0 backend-generated secrets are set. No new creds were provisioned; every live step below stays blocked. Populate these (values never printed by me) to unblock.
- **✅ RESOLVED (2026-08-27) — OWNERSHIP: CLAUDE.md's split stands.** The operator's "Two decisions" directive resolved the earlier conflict in favor of CLAUDE.md's documented split: Claude Code does NOT touch `app/` or `lib/wallets/` (Antigravity's). Claude Code's job is unblocking the backend + producing the per-screen wiring spec as a **standalone handoff document** (`docs/frontend-wiring-spec.md`), not writing frontend code. No `app/`/`lib/wallets/` files edited.
- **✅ RESOLVED (2026-08-27) — session-introspection endpoint added.** The "no way to reflect real login state across page loads (HttpOnly cookie)" blocker is closed via option (a): `GET /auth/session` → `{did, expiresAt}`, added to `docs/api-spec.md` FIRST (frozen-contract rule) then implemented (`app/api/auth/session/route.ts`, reusing `verifySessionToken`; 401 when no valid session). The frontend reads login state from this endpoint (see `docs/frontend-wiring-spec.md`).
- **Credential inventory (RESOLVED 2026-08-26).** `.env.local` has 13 key lines; a names-only, values-never-printed check confirms **only 4 are populated** — the Phase-0 backend-generated secrets (`BACKEND_ATTESTATION_SIGNING_KEY`, `ISSUER_ADMIN_KEY`, `DEMO_VERIFIER_KEY`, `SESSION_SECRET`). All 9 live-integration creds (`DATABASE_URL`, `MIDNIGHT_TESTNET_RPC`, `MIDNIGHT_WALLET_SEED`, `PROOF_SERVER_URL`, `ANON_AADHAAR_TEST_KEY`, `SOROBAN_RPC_URL`, `SOROBAN_TESTNET_SECRET`, `SEPOLIA_RPC_URL`, `SEPOLIA_DEPLOYER_KEY`) are EMPTY — so Phase 2 live migrate, Phase 5 Midnight, and Phase 6 deploy remain blocked on operator-provided values.
- **Docker daemon down + Proof Server unreachable** → blocks Phase 4 live CLI verify and Phase 5 deploy + proof generation.
- **Soroban/Sepolia deploy** → blocked until funded keys confirmed; contract source + local build + adapter unit tests (incl. the bad-signature negative test) are still authorable/runnable locally.
- **No external accounts yet** (Vercel, Supabase/Neon, Alchemy/Infura, GitHub) → blocks Phases 11–12.
- **✅ ACCEPTED (2026-08-27, session 5) — npm audit: 3 high-severity advisories, no dependency change.** All 3 highs trace to one transitive package, `deepmerge-ts@7.1.5` (GHSA-ggr8-5vv4-36mx — stack exhaustion on maliciously-recursive object graphs), pulled in via `@prisma/config` → `prisma` (Prisma 7's CLI/build-time config layer). No non-breaking fix exists (latest Prisma 7 still pins it; `--force` downgrades to Prisma 6; only forward path is the unstable `prisma@8.0.0-rc`). **Operator decision: accept as-is, do NOT downgrade Prisma** — it's a build-time config-merge path fed by our own `prisma.config.ts`, never request-input-reachable, with no production traffic (testnet MVP). Documented as an accepted finding in `docs/security-model.md` §AF-1, **flagged for mandatory re-evaluation before any real/mainnet deployment** (and no later than Prisma 8 stabilizing). The three libraries added for the adapters (`@noble/curves`, `viem`, `@stellar/stellar-sdk`) audit clean.

## Next Bash-gated actions (resumable checklist)
_Items 1 and 3 are DONE. Everything else is blocked on operator-provided credentials and/or a working compile toolchain (Bash was intermittently unavailable this session)._
1. ✅ **DONE** — `.env.local` checked (names + populated status only, no values): `DATABASE_URL` and all 8 other live creds are EMPTY (see Credential inventory above).
2. **BLOCKED on `DATABASE_URL`.** Once populated + DB reachable → `prisma migrate dev --name init` + a per-table insert/query → closes **Phase 2** live acceptance.
3. ✅ **DONE** — `vitest.config.ts` → `vitest.config.mts`; `npm test` runs clean (CJS/ESM warning gone).
4. **Phase 6 contracts — ✅ DONE (session 3):** `Registry.sol` + `soroban/src/lib.rs` bodies authored mirroring `lib/chains/attestation.ts` (recover signer, require signer == configured backend key, store result, reject duplicate `credentialId` + stale/future timestamp); Hardhat + Cargo projects scaffolded; **mandatory bad-signature negative test written + passing** on both chains (Soroban `cargo test` 7/7 + wasm build; EVM `hardhat test` 7/7). **Adapter encoding — ✅ DONE (session 4):** `evm.ts`/`soroban.ts` compute the canonical message + digest + freshness guard offline, unit-tested. **Sign + broadcast + read — ✅ DONE (session 5):** recoverable-secp256k1 signing via `@noble/curves` (`lib/chains/signing.ts` — EVM keccak256+eth-prefix → `r‖s‖v`; Soroban sha256 digest → `r‖s‖recovery`), broadcast/read wired via `viem` (Sepolia `simulate→write→wait`, `hasResult`/`getResult` reads) and `@stellar/stellar-sdk` (Soroban `prepare→sign→send→poll`, `simulate` read). Signing runs BEFORE the deployment gate, so a `ChainAdapterNotConfiguredError` can never mask a signing failure; independent recover-to-signer round-trips prove both signature layouts. `chains.test.ts` now 26 tests, full suite 80/80, tsc + eslint clean. **Only operational steps remain:** deploy the two registries + provision funded `SOROBAN_*`/`SEPOLIA_*` + `*_REGISTRY_*` addresses + RPC URLs — all operator-gated, no code left to write. **Deploy runbook authored (session 5): `docs/deployment.md`** — exact Hardhat + Stellar CLI steps, the three-key distinction (trust-root public half baked in vs. funded deployer/source keys), and capturing the addresses into `.env.local`.
5. **Phase 5 Midnight — circuit + witness ✅ DONE offline (session 6); live path BLOCKED:** `nivaan.compact` compiled + `witnesses.ts` + `nivaanCircuit.test.ts` (23 tests) prove the credential logic in-process via `@midnight-ntwrk/compact-runtime` (installed, with `@anon-aadhaar/core`). REMAINING (blocked): bring up Docker/Proof Server, generate + server-side verify a real Halo2 proof, deploy the compiled contract to Midnight testnet, then replace the `verifyProof` / `evaluateIssuanceCircuit` / wallet-signature seams with midnight-js impls. Needs `MIDNIGHT_*` / `PROOF_SERVER_URL` / `ANON_AADHAAR_TEST_KEY` + Docker.
6. **Phase 7 live acceptance (BLOCKED on 2 + 5 + 6):** REST-client full request/response cycle for all 8 endpoints against the real DB + Midnight + both chains.
7. **Demo-verifier same-origin proxy — ✅ DONE (session 5):** added `GET /api/demo-verifier/:id/result` to `docs/api-spec.md` FIRST (frozen-contract rule), then implemented `app/api/demo-verifier/[id]/result/route.ts`. The route takes NO key from the client — it reads `DEMO_VERIFIER_KEY` from its own server env as an enablement gate and calls the `getVerificationResult(id)` service directly (no self-HTTP hop), forwarding `{verificationId, chain, result, verifiedAt}` (`404` unknown id; the spec's `425` is documented-but-unreachable in the synchronous MVP). The secret never reaches the browser. Unblocks Antigravity's demo-verifier screen.

## In-flight research
- (resolved) Prisma 7.9 connection pattern — implemented + verified.
- (resolved) midnight-js verify API spike — delivered as leads (low confidence; see Phase 4). Re-confirm against installed `.d.ts` when Phase 5 installs the packages.
